# 오뚝이(Ottugi) — Streamlit Community Cloud 배포용 표준 pip 의존성.
#
# 로컬 macOS 개발은 libomp(OpenMP) 문제로 micromamba(conda-forge) 환경을 쓰지만,
# Streamlit Cloud는 리눅스라 이 문제가 없으므로 표준 pip requirements.txt만으로 충분하다.
# (로컬 개발 환경 재현 방법은 README.md 참고)
#
# 버전 핀은 "동작이 검증된 버전 이상 + 다음 메이저 미만" 범위로 느슨하게 잡았다.
# 검증에 사용한 정확한 버전: numpy 2.4.6 / pandas 3.0.5 / scikit-learn 1.9.0 /
# xgboost 3.2.0 / shap 0.51.0 / streamlit 1.61.1 / plotly 6.9.0 /
# jsonschema 4.26.0 / anthropic 0.121.0 (Python 3.11~3.13)

numpy>=2.1,<3
pandas>=2.2,<4
scikit-learn>=1.5,<2
xgboost>=2.1,<4
shap>=0.46,<1
streamlit>=1.40,<2
plotly>=5.24,<7
jsonschema>=4.20,<5
anthropic>=0.40,<1
