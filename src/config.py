"""오뚝이(Ottugi) 시뮬레이션/모델링 공통 설정값.

값의 근거는 모두 `오뚝이_서비스흐름_데이터설계.md` 3장/4장/8장을 따른다.
"""

# ---------------------------------------------------------------------------
# 시계열 길이
# ---------------------------------------------------------------------------
N_MONTHS = 12

# ---------------------------------------------------------------------------
# 3-2. account_master 파라미터
# ---------------------------------------------------------------------------
# 최소결제비율(m): 카드사가 부여하는 하한선, 계좌 개설 시 1회 고정 (계약 기간 내 불변)
M_MIN, M_MAX = 0.10, 0.30

# 수수료율(i): 여러 공시자료의 범위(15.18~18.45%, 16.09~18.4% 등)를 균등분포로
# 가정할 통계적 근거가 없다는 검토 결과에 따라 고정값(평균 근사치)을 사용한다.
INTEREST_RATE_FIXED = 0.165  # 16.5% 연이자율

# 문서에 명시된 민감도 분석용 참고 범위. 이자율 자체를 Uniform으로 샘플링하는 데는
# 쓰지 않고, 필요 시 별도 민감도 분석(시나리오 재계산)에서만 참고값으로 사용한다.
INTEREST_RATE_SENSITIVITY_RANGE = (0.15, 0.19)

# card_limit: 실제 통계 분포가 아닌 "서비스 동작 검증용 시나리오 파라미터"
CARD_LIMIT_SCENARIOS = [3_000_000, 4_000_000, 5_000_000, 6_000_000, 7_000_000]

# 3-3. revolving_fee(I_t) 일할계산 근사 일수. 실제 결제주기(28~31일)가 아닌
# MVP 단계의 월 단위 근사치이며, 실서비스 전환 시 실제 결제일 간격으로 대체해야 함.
DAYS_APPROX = 30
DAYS_IN_YEAR = 365

# 최소청구원금 하한 (KB국민카드 공식 산식: 최소청구원금과 5만원 중 큰 금액)
MIN_PRINCIPAL_FLOOR = 50_000

# ---------------------------------------------------------------------------
# 페르소나(위험군) 생성 비중 — 8장 저/중/고위험 로직을 참고한 생성 파라미터 세트.
# 이 자체가 모델 피처로 저장되는 것이 아니라, 합성 데이터의 위험 분포를 고르게
# 섞기 위한 생성 단계 메타데이터임 (account_master.persona_tier 참고용 컬럼).
# ---------------------------------------------------------------------------
RISK_TIER_WEIGHTS = {"low": 0.35, "medium": 0.35, "high": 0.30}

# 월별 결제 이행 이벤트 확률 (정상 / 최소결제만 이행 / 연체) — payment_status,
# delinquency_count_6m 파생변수가 의미를 가지려면 매월 100% 정상납부만 있어서는
# 안 되므로, 티어별로 소액의 이행 리스크를 확률적으로 부여한다.
# ⚠️ 근거 없는 시나리오 가정치(scenario assumption)임: 이 확률값을 뒷받침하는
# 공시 통계·논문 등 외부 근거는 없다. "고위험군일수록 연체 이벤트가 잦다"는
# 방향성만 반영한 임의 설정이며, 실서비스 전환 시 실제 연체율 통계로 재보정해야 한다.
PAYMENT_EVENT_PROBS = {
    "low": (0.97, 0.02, 0.01),
    "medium": (0.90, 0.07, 0.03),
    "high": (0.75, 0.15, 0.10),
}
PAYMENT_EVENTS = ("normal", "minimum_only", "delinquent")

# ---------------------------------------------------------------------------
# customer_master 범주형 분포
# ---------------------------------------------------------------------------
AGE_GROUP_WEIGHTS = {"20대": 0.32, "30대": 0.30, "40대": 0.22, "50대 이상": 0.16}
GENDER_WEIGHTS = {"M": 0.47, "F": 0.47, "미상": 0.06}
EMPLOYMENT_TYPE_WEIGHTS = {
    "정규직": 0.45,
    "비정규직": 0.15,
    "자영업": 0.20,
    "프리랜서": 0.12,
    "무직": 0.08,
}
MYDATA_CONSENT_RATE = 0.70

# ---------------------------------------------------------------------------
# 4-2. 위험 판정(risk_indicator) 규칙 파라미터
# ---------------------------------------------------------------------------
RISK_LEVEL_THRESHOLD_DEFAULT = 0.40  # 서비스 내부 운영 임계값 (공식 위험기준 아님)
RISK_LEVEL_THRESHOLD_SENSITIVITY = [0.35, 0.40, 0.45]
CARRYOVER_SHARE_OBSERVE_CUTOFF = 0.25
PAYMENT_RATIO_GAP_WARN_CUTOFF = 0.05
MINIMUM_PAYMENT_STREAK_SEVERE = 3

# 모델 입력 피처에서 제외되는 변수 (성별: 금융위 검증체계 근거로 리스크 변수 미사용,
# employment_type: 보조 세그먼트 변수로만 사용, 핵심 예측 피처 아님)
NON_MODEL_FEATURES = ["gender", "employment_type", "persona_tier"]

RANDOM_SEED = 42
